# Example: check storage for pending rewards
# For each player UUID in storage, give rewards if needed (requires you to track UUIDs)
# You can store rewards in storage and apply them here
# Then remove the pending flag
